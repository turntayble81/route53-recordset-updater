
//Set the following properties for the
//recordset which needs to be updated

const config = {
    recordsetName     : 'example.com',
    recordsetTTL      : 300,
    recordsetComment  : 'Some comment about record set',
    hostedZoneId      : 'Z8W873JNKE8S'
};

module.exports = config;